# RAG Engineering Lab

An agentic RAG workbench with full pipeline transparency and evaluation. Built with LangGraph, LangChain, Docling, and LangSmith.

🚧 Under active development — see `ARCHITECTURE.md` for the design and `docs/` for phase specs.